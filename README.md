Hi hope you are doing well.

We have completed the backend of you app. Here are the api list

1. User Authentication
   Create an acco-unt using e-m-ail and passwo-rd with OTP verification via the app.
   Secure pa-ssword reset via em-ail.
   User login using em-ail and passw-ord.
   Forgot pa-ssword with otp verification

2. Business Management
   Create, update, and delete a business.
   Retrieve all businesses with filters for the app.
   Retrieve a single business by ID.

3. Services
   Create and update services.
   Retrieve all services with filters for users.
   Retrieve a single service by ID.

4. Service Time Slots
   Define time slots for services.
   Create, update, and delete time slots for a service.

5. Service Portfolio
   Add portfolio images to a service.
   Remove portfolio images from a service.
6. Favorite Services
   Add a service to favorites.
   Remove a service from favorites.
   Retrieve all favorite services.

This is the api list the functional of the app. You will be able to see all this on next milestone when we implement them on app.

Delivery || lipbrowlash || leads bridge || FO22203009188
